const dataStore = {
  cache: {
    /*
     * processing
     * ready
     *
     * */
    state: null,
    data: null,
  },
  uiState: {
    /*
     * trendings
     * random
     * search
     * categories
     */
    state: null,
    error: null,
  },
};

export default dataStore;
